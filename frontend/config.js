// Edit these two lines after deploying your backend — nothing else needs to change.
window.SECUREPROMPT_API_BASE = "http://localhost:8000/api"; // e.g. "https://secureprompt-ai-gateway-backend.onrender.com/api"
window.SECUREPROMPT_API_KEY = "";           // must match backend API_KEY env var
window.SECUREPROMPT_DEMO_MODE = false;                        // set true for a public deployment (shows the demo banner)
window.SECUREPROMPT_GITHUB_URL = "";                          // e.g. "https://github.com/yourname/secureprompt-ai-gateway"
